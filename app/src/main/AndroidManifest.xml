<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exemple.andikwashuleni">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_daniel_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_daniel_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndikwaShuleni">
        <activity android:name=".NotificationActivity" />
        <activity android:name=".Developpeur" />
        <activity android:name=".Notification" />
        <activity android:name=".Dessinermoi" />
        <activity android:name=".photoPrendre" />
        <activity android:name=".photoActivity" />
        <activity android:name=".FormulaireTerminer" />
        <activity android:name=".FormulaireIndividuel" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>